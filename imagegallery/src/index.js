import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // You can create this file for custom styling if needed
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
